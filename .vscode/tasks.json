{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Django server",
            "type": "shell",
            "command": "cd .. && .venv\\Scripts\\activate.bat && cd NoteKeep_With_Django && python manage.py runserver",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
